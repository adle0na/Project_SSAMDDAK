using System;
using UnityEngine;

public enum SkillType
{
    ClassSkill,
    PublicSkill,
}

public enum SkillDirection
{
    Forward,
    Back,
    Self,
    Cross,
    XCross,
    AllDirection,
    WholeMap
}
